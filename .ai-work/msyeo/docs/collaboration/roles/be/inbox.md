# BE 수신함

## 받은 요청
| 요청 ID | 요청자 | 우선순위 | 상태 | 제목 | 선행 조건 |
| --- | --- | --- | --- | --- | --- |
| REQ-20260428-04 | PM | P1 | 완료 | 백엔드 인증/소유권 검증 테스트 보강 | 없음 |
| REQ-20260428-08 | PM | P1 | 완료 | 전역 예외 응답을 ApiResponse 규칙에 맞게 표준화 | 사용자 지시로 착수 |
| REQ-20260428-09 | PM | P1 | 완료 | 일기 생성/수정/삭제 API와 Flutter 플로우 연결 준비 | REQ-06, REQ-07, REQ-08 완료 |
| REQ-20260428-22 | PM | P1 | 완료 | API 검증 오류 계약 보강 | REQ-06, REQ-08 완료 |
| REQ-20260428-27 | 사용자/PM | P2 | 대기 | 사용성 개선용 API 후보 설계 | REQ-22 착수 또는 완료, REQ-09 범위 확인 |

## 현재 착수 후보
1. `REQ-20260428-27`: 검색/필터, 감정 통계, 위젯 요약 등 사용성 개선 API 후보 설계
2. `REQ-20260428-22`와 QA `REQ-20260428-29`는 완료됐다.

## BE 진행 메모
- `REQ-20260428-09` BE 분장:
  - 현재 Diary CRUD API의 생성/수정/삭제 요청/응답 DTO가 Flutter 연동에 충분한지 확인한다.
  - 생성/수정/삭제에서 access token 사용자 식별과 타 사용자 리소스 접근 차단 테스트를 보강한다.
  - 유효하지 않은 감정 코드, 날짜, 빈 제목/본문 등 요청 검증 기준이 필요하면 PM/QA에 계약 변경 요청을 남긴다.
  - 오류 응답은 `ApiResponse` wrapper와 HTTP status가 Flutter `ApiClient`에서 안정적으로 읽히는지 확인한다.
- `REQ-20260428-27` BE 요청:
  - 일기 검색/필터 API, 작성 검증 강화, 이미지 업로드 준비 API, 월간 감정 통계 API, 위젯용 요약 API, 토큰 만료/갱신 전략을 후보로 비교한다.
  - 후보별 API 경로/응답 초안, DTO/모델 변경 필요 여부, 테스트 보강 범위, 1순위 구현 요청 후보를 응답에 남긴다.
- PM 배분 메모:
  - `REQ-20260428-22`는 QA `REQ-20260428-29`의 선행 조건이므로 먼저 처리한다.
  - `REQ-20260428-27`은 설계 요청이므로 `REQ-20260428-22`와 파일 충돌이 없으면 이후 별도 블록으로 진행한다.
- QA 재현 메모:
  - calendar `year` 누락, `month` 누락, `month=13`은 현재 500 `Internal server error`로 내려온다.
  - calendar `year=abc` 타입 오류는 400 `Invalid request parameter`로 내려온다.
  - diary 생성 malformed JSON, `imageUrls` 타입 오류는 현재 500 `Internal server error`로 내려온다.
  - diary 생성 빈 `title/content/emotionCode`는 현재 200 생성 허용된다. QA가 생성된 검증 레코드는 삭제 완료했다.
  - `REQ-20260428-22`에서는 위 케이스를 400 계열 `ApiResponse`로 고정하거나, 빈 필드 허용 정책이면 PM/QA에 명시적으로 알려야 한다.
